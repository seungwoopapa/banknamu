# banknamu.com 연결 · 애드센스 검토 요청 안내서

사용자가 직접 해야 하는 일만 순서대로 적었습니다. 사이트 자체는 `docs/` 에 완성되어 있고 GitHub Pages 에 올라가 있습니다.

## 0. 현재 상태 확인

- GitHub 저장소: `https://github.com/seungwoopapa/banknamu`
- 임시 주소 `https://seungwoopapa.github.io/banknamu/` 는 쓰지 않습니다. CNAME 파일이 `banknamu.com` 으로 잡혀 있어서 도메인 연결 후에만 정상 동작합니다. 도메인 연결 전에는 로컬 미리보기로 확인하세요.

```bash
cd ~/banknamu-restore && python3 -m http.server 8766 --directory docs
```
브라우저에서 `http://localhost:8766` 을 열면 됩니다.

- **문의 이메일**: `config.json` 의 `contact_email` 이 비어 있으면 문의·개인정보처리방침 페이지에 "(이메일 준비 중)" 으로 나갑니다. 사용할 주소를 넣고 `python3 scripts/build.py` 후 푸시하세요. 애드센스 심사 전에 꼭 채워야 합니다.

## 1. DNS 설정 (도메인을 재구매한 등록기관에서)

도메인이 만료됐다가 재구매된 상태라 옛 레코드는 대부분 비어 있을 것입니다. 파킹 페이지용 A/CNAME 이 남아 있으면 지우고 아래를 추가합니다. (가비아 기준: My가비아 → 도메인 → `banknamu.com` → DNS 관리. 다른 등록기관이어도 레코드 내용은 같습니다.)

| 타입 | 호스트 | 값 | TTL |
|---|---|---|---|
| A | @ | 185.199.108.153 | 3600 |
| A | @ | 185.199.109.153 | 3600 |
| A | @ | 185.199.110.153 | 3600 |
| A | @ | 185.199.111.153 | 3600 |
| CNAME | www | `seungwoopapa.github.io.` | 3600 |

- A 레코드는 같은 호스트(@)로 4개 따로 추가합니다.
- CNAME 값 끝의 점(`.`)은 등록기관이 자동으로 붙이기도 합니다. 저장 후 `seungwoopapa.github.io` 로 표시되면 됩니다.
- 나중에 `xx.banknamu.com` 서브도메인을 다른 용도(티스토리 2차 도메인 등)로 쓸 때는 서브도메인별 CNAME 을 추가하면 되고, 위 레코드와 충돌하지 않습니다.

반영은 보통 10분~1시간, 길면 하루입니다. 확인:

```bash
dig +short banknamu.com A
```
185.199.108~111.153 네 개가 나오면 완료입니다.

## 2. GitHub Pages 에서 도메인 확인 · HTTPS 켜기

1. `https://github.com/seungwoopapa/banknamu/settings/pages` 접속.
2. **Custom domain** 에 `banknamu.com` 이 들어 있고 "DNS check successful" 이 뜨는지 확인. (CNAME 파일이 이미 있어서 자동으로 채워집니다. 비어 있으면 직접 입력 후 Save.)
3. DNS 확인이 끝나면 **Enforce HTTPS** 체크. 인증서 발급에 몇 분~1시간 걸립니다.
4. `https://banknamu.com/` 과 `https://www.banknamu.com/` 둘 다 열리는지, `https://banknamu.com/ads.txt` 가 텍스트 한 줄을 보여 주는지 확인.

## 3. 서치콘솔 등록 · 사이트맵 제출 (검토 요청 전에 꼭)

애드센스 심사는 구글이 사이트를 크롤링한 결과로 판단합니다. 색인되기 전에 검토를 요청하면 빈 사이트로 보고 반려될 수 있습니다.

1. search.google.com/search-console → 속성 추가 → **도메인** 방식으로 `banknamu.com` 입력.
2. 안내되는 TXT 레코드를 DNS 에 추가(호스트 @, 타입 TXT, 값 `google-site-verification=...`). 확인 버튼.
3. 색인 생성 → **사이트맵** → `https://banknamu.com/sitemap.xml` 제출.
4. **URL 검사**에 홈·글 서너 개 주소를 넣고 "색인 생성 요청". 하루 이틀 뒤 "페이지" 보고서에서 색인된 페이지 수가 올라가는지 확인.

예전에 `banknamu.com` 속성이 있었다면 사이트맵만 다시 제출하면 됩니다.

## 4. 애드센스 검토 요청

1. adsense.google.com → **사이트** → `banknamu.com`. 목록에 없으면 **사이트 추가**.
2. 사이트 모든 페이지 `<head>` 에 애드센스 코드가 들어 있습니다(`config.json` 의 `adsense_client` = `ca-pub-8336147776969406`. 옛 뱅크나무 원문에 박혀 있던 ID 와 같고, jaetechlab.com 과 같은 계정입니다).
3. `ads.txt` 도 같은 ID 로 자동 생성되어 있습니다. "ads.txt 문제" 표시는 1~2일 뒤 사라집니다.
4. 서치콘솔에서 색인된 페이지가 15개 이상 확인된 뒤 **검토 요청**. 결과는 보통 며칠, 길면 2~4주.
5. 결과가 나오면 구대리 세션(계정·수익)에 알려서 계정마스터에 등록합니다.

### 반려가 다시 나오면
- "가치가 별로 없는 콘텐츠": 이 사이트는 글 19편·텍스트 전용이라 jaetechlab 보다 얇습니다. 색인이 덜 됐으면 2주 더 기다렸다가 재요청하고, 그래도 반려면 새 글 5편 이상을 추가하는 것이 가장 확실합니다(`content/posts.json` 에 항목 추가 → `build.py`). 원문에 있던 표·이미지 자리를 표로 보강하는 것도 방법입니다.
- "사이트 다운 또는 사용 불가": DNS 나 HTTPS 문제. 2번 단계 재확인.
- "애드센스 코드 없음": `<head>` 의 코드 ID 가 계정과 다른 경우. 4-2 단계.

## 5. 그 다음

- 승인 뒤에도 사이트는 그냥 두면 됩니다. 사용자 본인 글이고 유지 비용이 없습니다.
- 글을 더 올리려면 `content/posts.json` 에 항목을 추가하고 `build.py` 를 돌린 뒤 푸시하면 됩니다. 형식은 기존 항목을 복사하면 됩니다.
